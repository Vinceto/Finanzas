{% extends 'base.html.twig' %}

{% block title %}Nueva Transacción{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-4">Nueva Transacción</h1>

    {{ form_start(form) }}
        <div class="row mb-3">
            <div class="col-md-6">
                {{ form_label(form.type, 'Tipo', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.type, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.type) }}
            </div>
            <div class="col-md-6">
                {{ form_label(form.amount, 'Monto', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.amount, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.amount) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-6">
                {{ form_label(form.date, 'Fecha', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.date, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.date) }}
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-12">
                <button class="btn btn-primary">Guardar</button>
                <a href="{{ path('transaction_index') }}" class="btn btn-secondary ms-2">Volver a la lista</a>
            </div>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
